<?xml version="1.0" encoding="UTF-8"?>
<project basedir="." name="copyfiles">
    <property name="editions" value="${basedir}/data/editions/"/>

    <delete dir="${editions}"/>
    <mkdir dir="${editions}"/>
    <copy todir="${editions}">
        <fileset dir="../brahms-presse/700_Werke/101_data_single">
            <include name="*.xml"/>
        </fileset>
    </copy>
</project>